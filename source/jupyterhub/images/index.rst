===========
User Images
===========

You can customize Jupyter Notebook environments for users using different user images. For example,
a user can select an alternate image for their notebook server that has a customized set of
packages.